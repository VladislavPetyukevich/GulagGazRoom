version: "3.8"

services:
  backend:
    container_name: "csharp"
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5043:5043"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development