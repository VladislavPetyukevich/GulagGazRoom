# GulagGazRoom Frontend

## Installation and run

Run `npm ci` for install all dependencies.  
Run `npm start` for start project in dev mode.  

## Building

Run `npm run build` for production-ready build.  
